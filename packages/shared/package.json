{
  "name": "@simracing/shared",
  "version": "2.2.10",
  "private": true,
  "description": "Shared types, DTOs and contracts for SimRacing Manager",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
