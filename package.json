{
  "name": "sim-center-manager",
  "version": "1.0.0",
  "description": "Plateforme de gestion pour centre de simulation automobile",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../agent && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:agent": "cd agent && npm run dev",
    "seed": "cd backend && npm run seed",
    "build": "cd backend && npm run build && cd ../frontend && npm run build && cd ../agent && npm run build"
  }
}
